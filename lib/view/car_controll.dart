import 'package:flutter/material.dart';

class CarControllPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("car")),
    );
  }
}
